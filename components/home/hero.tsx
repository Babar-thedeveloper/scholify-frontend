/**
 * Deprecated: the homepage hero now lives in `HeroSection.tsx`.
 * Re-exported here as `HomeHero` for backwards compatibility with any
 * existing imports. Prefer importing `HeroSection` directly.
 */
export { HeroSection as HomeHero } from "./HeroSection";
